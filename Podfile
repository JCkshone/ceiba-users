# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ceiba-test' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ceiba-test
  pod 'Alamofire', '~> 5.1'
  pod 'lottie-ios'

  target 'ceiba-testTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ceiba-testUITests' do
    # Pods for testing
  end

end
